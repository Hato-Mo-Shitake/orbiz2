import { ReactNode } from "react";
import { NoteEditor } from "src/orbits/contracts/note-orb";

export function FmAttrsEditor({
    editor,
    children
}: {
    editor: NoteEditor,
    children: ReactNode
}) {
    // 全部まとめて更新するボタンと、fmAttrEditorを囲う箱を設置予定。
}